# packages/config — Shared Configuration

Shared tooling configuration consumed by other workspaces: ESLint/Prettier
presets, TypeScript base `tsconfig`, and environment variable schemas.

> Scaffolding pending.
