# scripts — Repository Tooling

One-off and repeatable scripts for development and operations: environment
bootstrap, database reset/seed helpers, code generation (OpenAPI → types),
and release utilities.

Conventions: executable, self-documenting (`--help`), safe by default
(destructive actions require explicit flags).
